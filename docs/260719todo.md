# 260719 TODO

## 今日新增需求
- [ ] 修复并实现：主 Agent 发起子工单后自动触发被指派 Agent 执行（当前仅建单，状态停留 pending）
- [ ] 设计执行策略：创建子工单后是同步串行执行，还是异步队列执行
- [ ] 增加可观测性：在父工单消息中记录“子工单已启动/执行中/完成/失败”状态回写

## 亟待实现功能（高优先级）
- [ ] 子工单自动执行编排
  - 创建工单后自动调用对应 Agent 的 execute 入口
  - 对缺失 assignee、assignee 不在项目组、assignee 不可用做兜底处理
  - 防止循环派单（A->B->A）和无限递归建单

- [ ] 工单执行队列与并发控制
  - 同项目并发上限（避免模型/API Key 被打满）
  - 同 Agent 并发上限（避免一个 Agent 同时处理过多工单）
  - 重试策略（瞬时失败自动重试）与死信处理

- [ ] 工单状态机完善
  - pending -> in_progress -> reviewing -> completed 的状态迁移约束
  - 增加 blocked/failed 状态并记录失败原因
  - 父子工单联动状态（子工单失败时父工单提示风险）

- [ ] Agent 动作解析健壮性
  - 统一兼容 key:value 与 key=value 参数格式
  - 对多行参数、长文本参数增加稳定解析
  - 对无法解析动作给出可诊断错误，而非仅 finish

- [ ] 工具调用上下文补全
  - 对 get_project_members/create_ticket 统一支持从 ticketId 反推 projectId
  - 对 direct execute 接口增加 projectId/ticketId 透传校验
  - 对无上下文调用返回明确修复建议

- [ ] 审批流闭环
  - 审批通过后自动恢复原执行链路
  - 审批拒绝后给 Agent 明确 Observation 并引导替代方案
  - 审批中心增加“来源父工单/子工单”关联展示

- [ ] 前端协作体验
  - 工单详情页展示父子工单关系与跳转
  - 实时显示子工单执行进度（轮询或事件流）
  - Agent 动作/观察日志增加折叠与错误高亮

## 中优先级功能
- [ ] 自定义工具编辑能力（当前主要是新增/删除，缺少可视化参数编辑）
- [ ] Agent 配置页工具筛选（按分类、按审批要求）
- [ ] 工具调用失败的快速重试按钮（保留上次参数）

## 验收标准（针对“主Agent建单后自动执行”）
- [ ] 主 Agent 使用 create_ticket 成功后，子工单在 3 秒内进入 in_progress
- [ ] 被指派 Agent 自动开始执行，父工单可看到执行回写
- [ ] 子工单完成/失败后，父工单都有明确结果消息
- [ ] 不出现重复触发、循环触发、并发失控

## 备注
- 当前系统已支持：
  - get_project_members / create_ticket 工具
  - create_ticket 无显式 projectId 时从当前工单推断
- 当前缺口是：建单后未自动调度执行
