# index.ts

- 源文件：`apps/backend/src/config/index.ts`
- 文件职责：模块入口：负责初始化、装配或导出模块能力。
- 具名函数/方法：1 个

## 函数与方法

### `loadConfig`（第 24 行）

- 类型：函数
- 签名：`function loadConfig(): Config`
- 功能：读取或查询 config 数据并返回结果。
- 行为提示：访问文件系统。

---

> 本页由源码静态分析生成；行号以生成时版本为准。匿名内联回调归入其所属具名函数，不单独建条目。
