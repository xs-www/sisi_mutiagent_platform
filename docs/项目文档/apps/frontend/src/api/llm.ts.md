# llm.ts

- 源文件：`apps/frontend/src/api/llm.ts`
- 文件职责：前端 API 适配层：封装后端接口请求及其参数类型。
- 具名函数/方法：1 个

## 函数与方法

### `getProviderModels`（第 3 行）

- 类型：函数
- 签名：`export async function getProviderModels(provider?: string): Promise<Record<string, string[]> |`
- 功能：读取或查询 provider models 数据并返回结果。
- 行为提示：访问 HTTP/API。

---

> 本页由源码静态分析生成；行号以生成时版本为准。匿名内联回调归入其所属具名函数，不单独建条目。
