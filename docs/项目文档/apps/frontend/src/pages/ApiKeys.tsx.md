# ApiKeys.tsx

- 源文件：`apps/frontend/src/pages/ApiKeys.tsx`
- 文件职责：前端页面：组合数据请求、状态与用户交互界面。
- 具名函数/方法：13 个

## 函数与方法

### `ApiKeys`（第 38 行）

- 类型：React 组件
- 签名：`export default function ApiKeys()`
- 功能：渲染 ApiKeys 页面或界面组件，并协调其数据加载与交互状态。
- 行为提示：包含异步操作；更新界面或运行时状态。

### `handleCreate`（第 62 行）

- 类型：箭头函数
- 签名：`handleCreate()`
- 功能：处理 create 事件并更新相关状态。
- 行为提示：更新界面或运行时状态。

### `handleEdit`（第 69 行）

- 类型：箭头函数
- 签名：`handleEdit(record: ApiKey)`
- 功能：处理 edit 事件并更新相关状态。
- 行为提示：更新界面或运行时状态。

### `handleSave`（第 81 行）

- 类型：箭头函数
- 签名：`async handleSave()`
- 功能：处理 save 事件并更新相关状态。
- 行为提示：包含异步操作；更新界面或运行时状态。

### `handleDelete`（第 118 行）

- 类型：箭头函数
- 签名：`async handleDelete(id: string)`
- 功能：处理 delete 事件并更新相关状态。
- 行为提示：包含异步操作。

### `handleToggleActive`（第 128 行）

- 类型：箭头函数
- 签名：`async handleToggleActive(record: ApiKey, checked: boolean)`
- 功能：处理 toggle active 事件并更新相关状态。
- 行为提示：包含异步操作；更新界面或运行时状态。

### `render`（第 145 行）

- 类型：箭头函数
- 签名：`render(name: string)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

### `render`（第 152 行）

- 类型：箭头函数
- 签名：`render(provider: string)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

### `render`（第 160 行）

- 类型：箭头函数
- 签名：`render(key: string)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

### `render`（第 168 行）

- 类型：箭头函数
- 签名：`render(n: number)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

### `render`（第 175 行）

- 类型：箭头函数
- 签名：`render(active: boolean, record: ApiKey)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

### `render`（第 188 行）

- 类型：箭头函数
- 签名：`render(t: string)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

### `render`（第 194 行）

- 类型：箭头函数
- 签名：`render(_: any, record: ApiKey)`
- 功能：构建或格式化 render。
- 行为提示：未识别到显著的外部副作用。

---

> 本页由源码静态分析生成；行号以生成时版本为准。匿名内联回调归入其所属具名函数，不单独建条目。
