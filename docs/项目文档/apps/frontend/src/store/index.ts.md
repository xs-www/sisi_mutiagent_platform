# index.ts

- 源文件：`apps/frontend/src/store/index.ts`
- 文件职责：模块入口：负责初始化、装配或导出模块能力。
- 具名函数/方法：3 个

## 函数与方法

### `setCurrentProject`（第 17 行）

- 类型：箭头函数
- 签名：`setCurrentProject(project)`
- 功能：更新并保存 current project 状态或数据。
- 行为提示：未识别到显著的外部副作用。

### `setCurrentAgentId`（第 20 行）

- 类型：箭头函数
- 签名：`setCurrentAgentId(id)`
- 功能：更新并保存 current agent id 状态或数据。
- 行为提示：未识别到显著的外部副作用。

### `setPendingApprovalCount`（第 23 行）

- 类型：箭头函数
- 签名：`setPendingApprovalCount(n)`
- 功能：更新并保存 pending approval count 状态或数据。
- 行为提示：未识别到显著的外部副作用。

---

> 本页由源码静态分析生成；行号以生成时版本为准。匿名内联回调归入其所属具名函数，不单独建条目。
