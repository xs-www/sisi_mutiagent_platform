# 260801 TODO

> 从 260719todo.md 滚动而来。已实现的功能（子工单同步串行编排、状态机完善、SSE 分步 UI、工具上下文反推、前端父子工单跳转等）已移除，本清单仅保留尚未实现或待增强项。

## 亟待实现功能（高优先级）

- [ ] ReAct 监督机制
  - [ ] 在每轮 Thought / Action / Observation 之后自动做监督判断，决定继续执行、重试、进入 review 或终止（当前仅有 `maxIterations` 硬截断，缺少中途监督）
  - [ ] 对无效 Action、工具执行失败、循环派单、长时间无进展等情况生成可诊断的 Observation 和兜底建议
  - [ ] 将监督结果实时回写到工单消息与状态机，例如从 in_progress 进入 reviewing/failed/blocked
  - [ ] 支持父工单对子工单执行结果进行汇总与风险提示，避免错误链路继续传播

- [ ] Agent 动作解析健壮性（剩余项）
  - [ ] 对无法解析动作给出可诊断错误（当前 `action-parser.ts` 仅 `console.warn` 后默认 finish，Agent 与用户都难以察觉）
  - [ ] `message` 动作目前仅支持单行参数，对长文本 `content` 增加稳定解析

- [ ] 失败恢复与死信处理
  - [ ] 工具执行瞬时失败自动重试策略（保留上次参数）
  - [ ] 死信工单（多次失败/blocked）的人工介入入口与告警
  - 注：异步执行队列与同项目/同 Agent 并发上限已因「同步串行」策略不再需要，此处仅保留重试与死信

- [ ] 审批流闭环
  - [ ] 审批通过后自动恢复原执行链路（当前 `approval/routes.ts` 仅更新状态，不触发恢复）
  - [ ] 审批拒绝后给 Agent 明确 Observation 并引导替代方案
  - [ ] 审批中心增加「来源父工单/子工单」关联展示

## 中优先级功能
- [ ] 自定义工具编辑能力（当前主要是新增/删除，缺少可视化参数编辑）
- [ ] Agent 配置页工具筛选（按分类、按审批要求）
- [ ] 工具调用失败的快速重试按钮（保留上次参数）

## 备注
当前系统已支持：
- 子工单同步串行编排（`orchestration.ts`）：创建子工单后阻塞式 `await executeAgent`，保证 agent 间依赖顺序
- 循环派单检测（assignee 与触发 agent 相同 / 沿父链路向上遍历）
- assignee 兜底：缺失 assignee、agent 不存在、不在项目成员中均跳过执行并回写系统消息
- 工单状态机：`pending → in_progress → reviewing → completed`，含 `failed` / `blocked`，`executor.ts` 在失败时置 failed 并发射状态事件
- 父子工单联动：子工单完成/失败回写父工单消息
- SSE 分步 UI：`executor.ts` 通过 `onEvent` 透传 thought/action/observation，子工单事件带 `childTicketId` 标识供前端区分渲染
- per-ticket 写入互斥锁（`repository.ts` `withTicketLock`），避免并发回写丢消息
- 工具上下文反推：`resolveToolProjectId` 支持从 `ticketId` 反推 `projectId`
- 文件操作路径安全：`resolveSafePath` 禁止绝对路径与跳出工作目录
- Agent 文件操作限制在项目 `workspace` 目录
- 删除项目时清理 `projects/` 下对应数据
- 前端从项目主页可打开项目目录与工作空间
